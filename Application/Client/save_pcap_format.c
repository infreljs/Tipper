#include "pcap_interface.h"

int save_pcap_format(p_captured_list* data_list, const char* path)
{
    pcap_hdr_t glob_header;
    pcaprec_hdr_t packet_header;
    void* data;

    while(1)
    {
        
    }
}